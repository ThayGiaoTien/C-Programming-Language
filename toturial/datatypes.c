#include <stdio.h>
#include <stdlib.h>

int main(){
    int age=24;
    double gpa= 3.6;
    char grade= "A";
    char phrase[] = "Moscow aviation institute";
    // [] tells C that we want to string a bunch of characters into a string

    return 0;
}